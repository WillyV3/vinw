# Generated by distui - https://github.com/willyv3/vinw
# Edit as needed, distui won't overwrite manual changes

version: 2

before:
  hooks:
    - go test ./...
    - go mod tidy

builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    binary: vinw

archives:
  - format_overrides:
      - goos: windows
        format: zip
    files:
      - none*

checksum:
  name_template: 'checksums.txt'

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'

brews:
  - name: vinw
    repository:
      owner: willyv3
      name: homebrew-tap
      token: "{{ .Env.GITHUB_TOKEN }}"
    homepage: https://github.com/willyv3/vinw
    description: "vinw"
    license: MIT
    directory: Formula
    commit_author:
      name: distui
      email: distui@users.noreply.github.com
    commit_msg_template: "Brew formula update for vinw version {{ .Tag }}"
    test: |
      system "#{bin}/vinw", "--version"

# NPM publishing requires package.json in repo
# Run 'distui generate package.json' if not present

